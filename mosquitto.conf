# TCP listener (for local or internal use)
listener 1883
protocol mqtt
allow_anonymous true

# WebSocket listener (for public access via Railway)
listener 8080
protocol websockets
allow_anonymous true

# Persistence
persistence true
persistence_location /mosquitto/data/

# Logging to stdout (works well with Railway logs)
log_dest stdout
